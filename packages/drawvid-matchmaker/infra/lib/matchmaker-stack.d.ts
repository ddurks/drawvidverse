import * as cdk from 'aws-cdk-lib';
import { Construct } from 'constructs';
export declare class MatchmakerStack extends cdk.Stack {
    constructor(scope: Construct, id: string, gameKey?: string, props?: cdk.StackProps);
}
